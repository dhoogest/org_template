This directory contains some org-mode template files for work use. The idea is to have some templates for LaTeX and Beamer preformatting, then to create new procedure docs or presentations using that framework.
